name: Master

on:
  push:
    branches:
      - master

jobs:
  test_e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-go@v2
        with:
          go-version: '1.14'
      - uses: actions/checkout@v2
      - name: run e2e tests
        run: make test-e2e-all
